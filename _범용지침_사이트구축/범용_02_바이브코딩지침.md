# 바이브코딩 지침 — AI와 함께 사이트를 만들 때 (REV 1)

> **AI 협업 규칙 + SEO/AEO/GEO + 위험 관리.** 구조 규격은 `범용_01_사이트구조사양.md`를 따른다.
> 최종 개정: 2026-09-08

> **근거 수준 표기**
> `[검증]` 공식 문서나 직접 실측으로 확인됨 · `[추정]` 업계 통설이나 확증 없음 · `[효과없음]` 널리 퍼졌지만 근거가 없거나 이미 무력화됨
> **`[추정]`과 `[효과없음]`에 시간을 쓰지 말 것.**

---

## § 1. AI 협업 규칙 (이 문서에서 가장 중요한 장)

AI에게 코드와 문서를 맡기면 **사람이 손으로 할 때는 생기지 않던 사고**가 난다. 아래는 전부 실제로 겪은 것이다.

### 1.1 멈춰야 하는 지점 (STOP)

아래 상황에서 AI는 **추측하지 말고 멈춰서 사용자에게 확인**한다.

- 외부 데이터의 **컬럼 매핑** — 구조를 먼저 보고하고 매핑을 확인받은 뒤 진행
- **도메인·브랜드명** — canonical과 사이트맵 전체에 박히므로 빌드 전 확정 필수
- **가격·연락처·법인 정보** — 틀리면 신뢰 문제이자 법적 문제
- **동명이인·중복 데이터 처리** — 목록을 보여주고 확인받는다
- **URL 구조 변경, 새 섹션 타입 도입** 등 구조적 결정
- 판단이 애매한 모든 것

각 프로젝트 작업지시서에 **"사용자에게 확인받아야 하는 항목"** 절을 두고 여기 해당하는 것을 나열한다.

### 1.2 추정값 금지

- **빈 값은 공란으로 둔다.** 생성기가 해당 줄을 렌더하지 않게 만든다
- 통계·인용구·사례를 **AI가 지어내지 않는다.** 프롬프트에 "제공된 데이터만 사용할 것"을 명시
- 확인 안 된 수치에 "약", "여" 같은 말을 붙여 사실처럼 만들지 않는다

### 1.3 체크리스트는 현황 기록이지 할 일 목록이 아니다

프로젝트 문서의 체크리스트는 **"지금 이렇다"**를 적은 것이지 **"이렇게 해야 한다"**가 아니다.

- 미체크 항목을 발견해도 **임의로 구현하지 않는다.** 개선이 필요해 보이면 계획을 먼저 제시하고 승인받는다
- **단, 새 페이지를 신규 작성할 때는 예외** — title·meta description·alt 텍스트·canonical 등 기본 필수 항목은 승인 없이 바로 채운다. 기존 구조를 새로 설계해야 하는 것(스키마 추가, URL 정책 변경)만 사전 계획 대상이다

이 두 줄이 **"시키지 않은 일을 하는 AI"와 "아무것도 못 하는 AI"** 사이의 경계다.

### 1.4 기록 규칙

- 매 세션 끝에 작업지시서 **작업 로그**에 한 줄: 날짜 / 한 일 / 다음 할 일 / 막힌 점
- **실패와 사고를 반드시 남긴다.** 다른 프로젝트에서 같은 실수를 반복하지 않으려면 이게 유일한 수단이다
- 결정을 **번복할 때는 이유를 함께** 남긴다. 안 그러면 다음 세션에서 같은 논쟁을 다시 한다

---

## § 2. 하나의 가시성 레이어

SEO·AEO·GEO는 **별개의 최적화 채널이 아니다.**

- Google AI Overviews는 **기존 검색 랭킹 위에 얹혀** 동작한다 `[검증]`
- ChatGPT Search·Perplexity의 인용도 **자체 검색 인덱스에서의 순위**가 1차 관문이다 `[추정]`

**"AEO 전용 작업", "GEO 전용 작업"이라는 것은 대체로 존재하지 않는다.** 검색에서 잘 잡히지 않는 페이지는 AI도 인용하지 않는다. 이 프레임을 잘못 잡으면 스키마만 잔뜩 붙이고 정작 콘텐츠가 없는 페이지를 만든다.

### 2.1 개발 우선순위

1. **SSR / 정적 HTML로 본문 노출** `[검증]`
   대부분의 AI 크롤러(GPTBot, PerplexityBot 등)는 **JavaScript를 실행하지 않는다.** CSR로만 그려지는 페이지는 AI 입장에서 본문이 존재하지 않는다. **실무에서 가장 흔하고 가장 치명적인 병목.**
   → 검증법: `curl` 로 받아 본문이 보이는지 직접 확인
2. **읽을거리 확보** `[검증]`
   히어로 문구와 입력 폼만 있는 페이지는 검색엔진이 주제를 판단할 재료가 없다. 사용 방법·FAQ·용어 설명 등 **사람이 읽을 본문**이 스키마보다 먼저다
3. **Answer-First 문장 구조** `[검증]`
   제목 바로 아래 질문의 답을 먼저, 근거는 뒤에
4. **구조화 데이터(JSON-LD)** `[추정]`
   기계 가독성엔 도움이나 **구글은 구조화 데이터가 랭킹 요인이 아님을 반복해 밝혔다.** 1~3을 끝낸 뒤의 마무리 작업

### 2.2 Answer-First 작성 기준

- **길이는 공백 포함 150~200자** `[검증]`
  흔히 인용되는 "50단어"는 **영어권 기준**이라 한국어에 그대로 적용되지 않는다
- 핵심 답을 첫 문장에 두고, 부연은 다음 문단으로 분리

**FAQ 리치 결과에 대한 기대치** `[검증]`
구글은 **2023년 8월 FAQ 리치 결과를 정부·의료 등 권위 있는 사이트로 제한**했다. 일반 상업 사이트는 `FAQPage` 마크업을 넣어도 **검색결과에 아코디언이 표시되지 않는다.**
그럼에도 FAQ를 만드는 이유는 **본문 콘텐츠가 생기기 때문**이고, 구조화 데이터는 AI가 Q&A 쌍을 파싱하는 데 도움이 된다. **목적을 혼동하지 말 것.**

### 2.3 콘텐츠 문서 작성 지침

**아래 규칙을 그대로 따르면 된다.** 실제로 상위에 있는 문서들을 해부해 공통점을 뽑아낸 결과이며, 문서를 쓸 때마다 경쟁 문서를 다시 분석할 필요는 없다. `[검증]` (2026-09-08 도출)

> 재분석이 필요한 때는 따로 있다. 이 규칙대로 썼는데도 결과가 안 나오거나, 검색엔진의 노출 방식이 눈에 띄게 바뀌었을 때다. 그때만 상위 문서를 다시 받아 이 절을 갱신한다.

**① 이름 붙은 분류 프레임을 제시한다**

"일곱 갈래", "데이터 5가지"처럼 **세어지는 분류 체계**가 상위 문서의 공통 뼈대였다. 단순한 순서(1단계·2단계)와는 다르다. 분류에는 이름이 있어야 하고 개수가 고정돼야 한다.

이유는 분명하다. **AI가 요약할 때 뽑아갈 덩어리가 생기기 때문**이다. 이름 없는 서술은 인용할 단위가 없어 통째로 버려진다.

**② 자사 실측 수치를 본문에 넣는다**

일반론은 누구나 쓸 수 있어 순위를 만들지 못한다. 직접 측정한 숫자가 그 문서의 존재 이유가 된다.

> ⚠️ **없는 숫자를 지어내지 말 것**(§1.2). 경쟁 문서가 쓴 수치를 흉내 내는 것이 가장 위험하다. **우리가 실제로 가진 데이터만 쓰고, 없으면 그 주제를 쓰지 않는다.**

**③ 숫자의 출처를 별도 절로 밝힌다**

본문 끝에 "이 글에 쓰인 숫자의 출처" 절을 두고 표본 수, 수집 시점, 집계 기준, **그리고 아직 결론을 낼 수 없는 항목**을 적는다. 한계를 함께 밝히는 쪽이 신뢰를 얻는다.

**④ FAQ에 브랜드 질문을 하나 넣는다**

"○○는 이 문제를 어떻게 진단하나요?" 형태의 문항을 마지막에 둔다. AI가 FAQ를 인용할 때 **자사명이 답변에 함께 들어간다.** 단 광고 문구가 아니라 실제 방법을 설명하는 답이어야 한다.

**⑤ 분량은 경쟁 문서를 재서 정한다**

절대 기준은 없다. 다만 상위 문서 대비 **절반 이하면 그 자체가 불리**하다. 분량이 모자라면 문장을 늘리지 말고 **넣을 데이터를 먼저 확보**한다(사양 §2.2).

**⑥ Answer-First 블록에 id를 부여한다**

첫 문단에 `id="aeo-direct-answer"` 같은 식별자를 주면 문서 안에서 답변 위치가 명시된다. 상위 문서가 실제로 쓰고 있던 방식이다.

### 2.4 스키마에 대한 주의 `[효과없음]`

- `Article`의 `citation`은 실존하는 속성이지만 **구글이 랭킹에 쓴다는 근거가 없다**
- `about`에 `Dataset`을 중첩하는 것은 **잘못된 사용법**이다. Dataset 마크업은 Dataset Search용이며 독립 타입으로 발행해야 의미가 있다
- **EEAT는 랭킹 신호가 아니라 평가자 가이드라인의 개념**이다. 스키마에 저자를 적는다고 신뢰도가 생기지 않는다. 실제로 작동하는 것은 그 저자의 **외부 평판**이다
- **시맨틱 태그(`article`, `section`)** — 접근성에는 확실히 좋으나 **순위 효과 근거는 없다**

---

## § 3. 대량 생성의 위험

AI는 템플릿 페이지를 수백 장 찍어내기가 너무 쉽다. 아래는 그때만 생기는 사고다.

### 3.1 대량생성 콘텐츠 정책 `[검증]`

구글의 **scaled content abuse** 정책은 "가치 추가 없이 대량 생성된 페이지"를 직접 겨냥한다.

**방어**
- **발행 조건을 코드에 박는다** — 고유 데이터가 없으면 생성하지 않거나 `noindex` (사양 §2.1)
- **문장이 아니라 데이터가 다양성을 만든다** (사양 §2.2)
- **페이지 수를 욕심내지 않는다.** 수십~수백 장은 버티지만 수천 장은 위험이 급격히 올라간다

### 3.2 발행 분산 `[검증]`

수백 장을 하루에 쏟아내면 그 자체가 신호가 된다. **하루 2~3장씩 나눠 공개**한다.

정적 사이트 생성기에서 자동화하는 방법:
- 각 페이지에 **미래 날짜**를 분산해 넣고 커밋
- 생성기가 **미래 날짜 글을 발행 제외**하도록 설정 (Hugo `buildFuture = false` 등, 대개 기본값)
- **매일 자동 재빌드**를 걸어두면 날짜가 지날 때마다 하나씩 공개된다

> ⚠️ **과거 사고**: 한 프로젝트에서 `buildFuture = true`로 잘못 설정해 이 메커니즘이 통째로 무력화되어 대기 중이던 페이지가 전부 즉시 공개됐다. 이 설정을 켜지 말 것.

### 3.3 중복 URL `[검증]`

입력 변형만큼 페이지가 생긴다. 띄어쓰기·대소문자·인코딩·쿼리 순서가 각각 다른 URL이 되어 같은 내용이 여러 번 색인된다.

**방어**: 정규화 함수로 대표 표기 하나만 발행하고, 변형 요청은 `<link rel="canonical">`로 대표 URL을 가리킨다.

> 실제 사례: `안산 상록구맛집`과 `안산 상록구 맛집`이 각각 페이지로 잡혀 사실상 같은 내용 2장이 색인될 뻔함 (2026-09-08).

### 3.4 고아 페이지 `[검증]`

사이트맵에는 있는데 **어디서도 링크되지 않는** 페이지가 양산된다. 사이트맵은 발견 수단일 뿐이고 내부 링크는 **권위 전달 통로**다.

**방어**: 자동 생성 페이지는 반드시 **허브 페이지**를 함께 만들고, 그 허브를 홈이나 푸터에서 링크한다 (사양 §1.7).

---

## § 4. AI 크롤러와 공개 범위

### 4.1 robots.txt의 AI 크롤러 정책 `[검증]`

GEO를 하겠다면서 robots.txt를 방치하는 경우가 많다. **인용되고 싶다면 허용해야 한다.**

주요 크롤러: `GPTBot`(OpenAI 학습), `OAI-SearchBot`(ChatGPT 검색), `ClaudeBot`(Anthropic), `PerplexityBot`, `Google-Extended`(구글 AI 학습 제어)

**허용하면 인용 가능성이 생기고, 차단하면 콘텐츠를 지킨다. 둘 다 가질 수 없다.** 의식적으로 선택하고 이유를 작업지시서에 남긴다.

### 4.2 공개 범위의 전략적 판단 `[검증]`

일반 SEO 지침은 "많이 노출하라"고만 말한다. 하지만 **색인시킨다는 것은 경쟁사에게 데이터를 넘긴다는 뜻**이기도 하다. AI가 요약까지 해서 떠먹여 주므로 노출 비용은 예전보다 커졌다.

**자산 판별 기준**

| 유형 | 자산인가 | 처리 |
|---|---|---|
| 이미 공개된 사실을 모아놓은 것 | ✕ | 공개해도 잃을 게 없다 |
| 우리만 축적한 시계열·변화량 | ○ | 신중히 |
| 그 변화의 **원인·해석** | ★ 핵심 | 공개하면 상품 자체를 무료로 주는 것 |

**"SSR = 색인"이 아니다.** 크롤러가 읽게 하되 색인은 막는 조합(`noindex`)도 선택지다.

---

## § 5. 법적 리스크 (규제 업종은 표현 자체가 위법이 될 수 있다)

**대량 자동생성과 법령 위반이 만나면 위반 페이지가 한 번에 수백 장 생긴다.** 적발 시 전량 수정·삭제이며 행정처분과 형사처벌 대상이 될 수 있다.

### 5.1 착수 전 판정

작업지시서 §1에 **`규제업종 여부`**를 반드시 적는다. 해당하면 **문구 자동생성을 금지**하고 전량 사람이 검수한다.

| 업종 | 근거 법령 | 대표 금지 |
|---|---|---|
| 의료·병원·한의원·치과 | **의료법 §56(의료광고)** | 치료경험담·환자 후기, **최상급 표현**("최고·1위·유일·완치"), 비급여 진료비 **할인·이벤트로 환자 유인**, 시술 전후 사진, 다른 의료기관과의 비교, 비의료인의 의료광고. **사전심의 대상 매체는 심의 없이 게재 불가** |
| 변호사·법률사무소 | **변호사법 §23(광고)**, 대한변협 광고규정 | 승소율·수임건수 등 **객관적 검증이 어려운 실적 표시**, 사건 유치 목적의 **알선·유상 소개**, 특정 결과를 보장하는 표현 |
| 공인중개사·부동산 | **공인중개사법 §18의2(표시·광고)** | 광고에 **중개사무소 명칭·소재지·등록번호·개업공인중개사 성명 미표기**, **존재하지 않는 매물**·거래 불가 매물 광고, 가격·면적 등 **사실과 다른 표시** |
| 건강기능식품·식품 | **식품표시광고법 §8** | **질병의 예방·치료 효능** 표현, 의약품 오인 표현, 체험기 형태의 효능 광고 |
| 화장품 | **화장품법 §13** | 의약품으로 오인시키는 표현, 인증받지 않은 기능성 표방 |
| 의약품·의료기기 | **약사법·의료기기법** | 광고 사전심의 대상. 효능 과장 |
| 학원·교습 | **학원법**, 표시광고법 | 합격률·취업률 등 **근거 없는 실적** |
| 금융·대부 | **대부업법·금소법** | 이자율·조건 표시 의무, 과장 광고 |

### 5.2 업종 불문 공통 금지

- **최상급·단정적 표현**: "최고", "1위", "유일", "100% 보장", "완벽한" — 객관적 근거와 출처가 없으면 **표시광고법상 부당광고**
- **근거 없는 비교광고**: 경쟁사를 지목해 우열을 표시
- **후기·체험담의 광고 활용**: 업종에 따라 그 자체가 금지되며, 대가를 받은 후기는 **대가성 표시 의무**
- **거짓·과장 실적**: "누적 N건" 등은 실제 수치와 근거를 보관

### 5.3 타인·타사에 대한 서술

- **타사 상호와 함께 부정적 평가를 공개 색인**시키면 **신용훼손·명예훼손** 시비가 될 수 있다 `[검증]`
- 원칙: **공개 페이지에는 중립적 사실과 수치만.** 등급·판정·부정적 해석은 비공개 영역(로그인·`noindex`·내부 리포트)에 둔다
- 개인 이름·연락처 등 개인정보는 공개 페이지에 싣지 않는다

### 5.4 코드로 강제할 것

- config에 `regulated: true` 같은 플래그를 두고, **켜져 있으면 금칙어 목록으로 빌드 시 스캔해 발견 시 빌드를 실패**시킨다
- 금칙어 예: `최고`, `1위`, `유일`, `완치`, `100%`, `보장`, `부작용 없`
- 스캔은 **자동생성 문구뿐 아니라 사람이 쓴 문장도 포함**한다

---

## § 6. 색인 투입 (이 단계를 빠뜨리면 앞의 전부가 무의미)

만들어 놓고 색인이 안 되면 존재하지 않는 것과 같다. **손으로 하루에 몇 개씩 누르지 않는다.**

### 6.1 IndexNow — 대량 통보의 공식 경로 `[검증]`

- 네이버는 **2023-07-26부터 IndexNow를 지원**한다. 빙·얀덱스에도 함께 전달된다
- 수천 개 URL을 **POST 한 번**으로 통보할 수 있다

절차
1. **키 발급(최초 1회)** — 32자 키를 만들고 `사이트루트/<키>.txt`로 배포. 키는 재사용하며 매번 새로 만들지 않는다
2. **키 확인** — 배포된 사이트에서 `https://도메인/<키>.txt`가 열려야 한다. 여기서 실패하면 전송이 **403**으로 거절된다
3. **드라이런 → 실제 전송** — 무엇이 나갈지 먼저 확인하고 보낸다. 조심스러우면 300개씩 나눠 보낸다
4. **로그 보관** — 전송 일시·건수·응답 코드를 파일로 남긴다. 2~4주 뒤 순위 관측의 기준선이 된다

응답 코드: `200/202` 정상 · `403` 키 실패 · `422` host 불일치(사양 §1.1) · `429` 과다 요청

> IndexNow는 **변경 통보**이지 색인 보장이 아니다.

### 6.2 서치콘솔·서치어드바이저는 병행한다 (대체 아님)

1. 사이트 등록 → 소유확인 → **사이트맵 제출**(필수, IndexNow와 별개)
2. **수동 수집요청은 핵심 페이지 30~50개에만** 쓴다
3. 구글·네이버·다음 각각 제출
4. **등록 자체는 이른 시점에 해도 불이익이 없다.** 다만 **내용이 빈 페이지를 색인 요청**하면 "크롤링됨–색인 생성 안 됨"에 오래 머물 수 있으니, **등록은 일찍, 개별 색인 요청은 내용을 채운 뒤**

### 6.3 하면 안 되는 자동화 `[검증]`

**서치어드바이저·서치콘솔 로그인 세션을 Selenium·Playwright로 조작해 수집요청 버튼을 반복 클릭하지 않는다.** 이용약관 위반이고 **계정 자체가 제재 대상**이 된다. IndexNow가 공식 경로로 열려 있는 이상 감수할 이유가 없다.

---

## § 7. 운영 안전장치

### 7.1 비밀정보

- 배포훅 URL, API 키, 분석 ID, 인증 코드는 **코드에 하드코딩 금지**. 저장소 시크릿·환경변수로만 관리
- 이미 하드코딩된 것을 발견하면 **기록만 하고 임의로 옮기지 않는다** — 배포 파이프라인 변경은 사용자 확인 대상
- 커밋 전 `git status`로 포함 파일을 확인한다. 파일명이 무난해 보여도 내용을 본다

### 7.2 데이터 부패

수집 데이터는 시간이 지나면 틀린 정보가 된다. **프로젝트마다 정한다.**
- 갱신 주기와 방법
- 갱신이 끊긴 데이터의 **유효기간**과 그 이후 처리(페이지 내림 / `noindex` / 기준일 명시)
- **모든 데이터 페이지에 기준일을 표시**한다

### 7.3 롤백

- 배포 전 **되돌리는 방법**을 알고 있을 것 (이전 배포 복원 / revert 커밋 / 백업 폴더)
- 되돌릴 수 없는 작업(URL 정책 변경, 대량 삭제, 강제 푸시)은 **실행 전에 사용자 확인**
- 대량 변경 전 산출물 폴더를 백업한다

### 7.4 검증 자동화

빌드 후 **스크립트로** 확인하고 결과를 보고한다. 눈으로 세지 않는다.

- [ ] 생성된 URL 수 = 예상 수치와 일치
- [ ] 중복 슬러그 0건
- [ ] 모든 내부 링크의 대상이 실제로 존재 (깨진 링크 0건)
- [ ] 사이트맵 URL 수 = 실제 생성된 페이지 수
- [ ] 고유 데이터가 비어 얇아진 페이지 비율 — 임계치 초과 시 보고 (사양 §2.1)
- [ ] 금칙어 스캔 통과 (§5.4)
- [ ] 임의 페이지 3장을 실제로 열어 렌더링 확인

---

## § 8. 최종 체크리스트

**반드시 할 것** `[검증]`
- [ ] 핵심 본문이 **JS 없이** HTML 소스에 보이는가 (`curl`로 확인)
- [ ] 사람이 읽을 본문이 충분한가
- [ ] 자동 생성 페이지에 **발행 조건**이 걸려 있는가 (사양 §2.1)
- [ ] 문장 다양성이 **데이터에서** 나오는가 (사양 §2.2)
- [ ] 중복 URL이 canonical로 통합되는가
- [ ] 자동 생성 페이지가 **내부 링크로 연결**되어 있는가
- [ ] robots.txt의 AI 크롤러 정책을 **의식적으로 결정**했는가
- [ ] 공개하면 안 되는 데이터가 색인되고 있지 않은가
- [ ] **규제 업종 판정**과 금칙어 스캔을 했는가 (§5)
- [ ] 비밀정보가 코드에 없는가
- [ ] AI가 통계·사례를 **지어내지 않았는가**
- [ ] 색인 투입과 사이트맵 제출을 마쳤는가

**해도 되지만 기대는 낮출 것** `[추정]`
- [ ] JSON-LD 구조화 데이터
- [ ] 시맨틱 태그 (접근성 목적으로는 권장)
- [ ] 저자 정보 표기 (사용자 신뢰용)
