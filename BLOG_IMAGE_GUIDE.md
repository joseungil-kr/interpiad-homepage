# 인터피아드 블로그 이미지 생성 및 Alt 태그 운영 규칙

이 문서는 인터피아드(interpiad.com) 블로그 포스트를 발행하거나 수정할 때 적용해야 하는 **이미지 생성, 경로 배치, 그리고 접근성/SEO용 `alt` 태그 설정 규칙**을 정의합니다.

---

## 1. 이미지 생성 규칙 (Image Generation Guide)

1. **디자인 컨셉 및 브랜드 아이덴티티 유지**:
   - **기본 톤**: 신뢰감 있는 B2B 전문 에이전시 무드 (2002년부터 이어온 20+년 업력의 진중함과 전문성 반영).
   - **컬러 팔레트**:
     - Background: 딥 잉크 네이비 (`#14213D`) 또는 따뜻한 프리미엄 데스크/오피스 무드
     - Point/Accents: 브라스 골드 (`#B08A4E`), 클린 화이트 (`#F7F5F1`), 은은한 사이버 블루
   - **스타일 및 인물/배경 가이드 (필수)**:
     - **인물 모델링**: 20년 업력의 전문성과 신뢰감을 주는 **한국인 남성 전문가/컨설턴트/강사** (30~40대 단정한 비즈니스 정장 또는 셔츠 차림, 태블릿/노트북으로 차트를 짚어주거나 브리핑하는 모습).
     - **배경**: **한국적인 현대 비즈니스 공간** (서울 도심 빌딩 전경이 보이는 오피스, 세련된 회의실, 로컬 비즈니스 상권 현장 등).
     - **컬러/무드**: 딥 잉크 네이비와 브라스 골드 조명, 신뢰감 있고 묵직한 전문가의 분위기.
     - 단순 스톡 사진 느낌을 지양하고, 왜곡 없는 정돈된 고품질 비주얼 연출.

3. **블로그 본문 공통 안내 규칙 (Action 기반 과금 안내)**:
   - 모든 교육, 컨설팅, 진단 관련 포스트 하단에는 다음 원칙을 명시한다:
     > *"전화상담은 일체 무료입니다. 실제 1:1 맞춤 교육, 심층 컨설팅, 홈페이지 제작, 블로그 포스팅 등의 실제 실행(Action)이 진행될 때만 사전 협의된 소정의 비용이 청구되며, 상담 자체만으로는 어떠한 비용도 발생하지 않습니다."*

2. **기술 규격**:
   - **비율(Aspect Ratio)**: `16:9` 가로형
   - **권장 해상도**: 가로 960px 이상 (Hugo 레이아웃 기준 `width="960" height="540"`, 썸네일 기준 `width="480" height="270"`)
   - **파일 포맷**: `.jpg` (경량화 및 범용 웹 브라우저 호환)

---

## 2. 파일 저장 위치 및 매핑 규칙

Hugo 템플릿의 자동 이미지 연동 구조에 따라, 마크다운 포스트의 파일명(슬러그)과 동일한 이름의 이미지 파일을 `static/images/blog/`에 배치합니다.

* **저장 디렉토리**: `static/images/blog/`
* **네이밍 규칙**: `[포스트-슬러그].jpg`
  - 예시:
    - 포스트: `content/blog/semi-vs-optimized-blog-cost.md`
    - 이미지: `static/images/blog/semi-vs-optimized-blog-cost.jpg`

> 템플릿 로직(`layouts/blog/single.html` 및 `layouts/partials/blog-card.html`)에서 `static/images/blog/[slug].jpg` 파일이 감지되면 상세 페이지 메인 대표 이미지 및 블로그 목록 카드 썸네일로 자동 출력됩니다.

---

## 3. 이미지 Alt 태그(대체 텍스트) 및 SEO 접근성 규칙

검색엔진 최적화(SEO)와 웹 접근성(a11y) 기준을 준수하기 위해 모든 이미지에 명확한 대체 텍스트를 제공합니다.

1. **블로그 상세 페이지 대표 이미지**:
   - `alt="{{ .Title }} 이미지"` (Hugo 템플릿 자동 적용)
   - 본문 내 수동 삽입 시: `![키워드가 포함된 이미지의 구체적인 설명](/images/blog/파일명.jpg)` 형식 준수.

2. **블로그 목록 카드 썸네일**:
   - `alt="{{ $p.Title }} 썸네일"` (화면 낭독기 및 검색 로봇이 어떤 글의 미리보기인지 식별 가능하도록 구성)

3. **사이트 전역 이미지 점검 기준**:
   - 로고: `alt="인터피아드"`
   - 순수 장식용 배경 이미지(Hero 배경 등): `alt="" aria-hidden="true"`로 스크린 리더에 잡음이 되지 않도록 처리.
   - 정보성 이미지: 해당 이미지가 전달하는 핵심 정보를 1~2문장의 명확한 한국어 텍스트로 기재.

---

## 4. 발행 및 점검 체크리스트 (Workflow Checklist)

1. [ ] `content/blog/[slug].md` 포스트 작성 완료
2. [ ] 16:9 비율의 블로그 대표 이미지 생성 완료
3. [ ] `static/images/blog/[slug].jpg` 경로로 파일 복사
4. [ ] 본문에 삽입된 이미지가 있는 경우 `alt` 속성 누락 여부 확인
5. [ ] `hugo` 로컬 빌드 테스트 (`public/` 디렉토리에 정상 반영 확인)
6. [ ] `node scripts/indexnow-submit.js` 실행하여 검색엔진에 신규 URL 즉시 색인 요청
7. [ ] git commit 및 push로 Cloudflare Pages 자동 배포 완료
